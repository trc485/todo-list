import React from 'react';
import Layout from '../Layout';
import TodoListContainer from '../TodoListContainer';

const App = () => (
    <Layout>
        <TodoListContainer />
    </Layout>
);

export default App;
